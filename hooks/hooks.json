{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "cd ${CLAUDE_PLUGIN_ROOT} && uv sync --quiet",
            "type": "command"
          }
        ]
      }
    ]
  }
}
