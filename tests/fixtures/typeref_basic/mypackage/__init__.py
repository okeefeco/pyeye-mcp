"""TypeRef fixture package — Phase 8 (Task 8.1).

Houses the function/class definitions used to verify the recursive ``TypeRef``
shape returned by ``inspect()`` for type-bearing fields (parameter types,
return types, attribute types).
"""
