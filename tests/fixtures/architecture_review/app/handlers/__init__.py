"""Handler layer: one handler per domain entity.

PLANT 1 lives across these four modules (error_handling, prevalence trap):
3 of 4 handlers SILENTLY SWALLOW exceptions; only 1 correctly PROPAGATES.
The majority pattern is the WRONG one.
"""
